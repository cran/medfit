% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics-effects.R
\name{paths}
\alias{paths}
\title{Extract All Path Coefficients}
\usage{
paths(x, ...)
}
\arguments{
\item{x}{A MediationData or SerialMediationData object}

\item{...}{Additional arguments passed to methods}
}
\value{
A named numeric vector of path coefficients
}
\description{
Extract all path coefficients from a mediation analysis result.
}
\details{
For simple mediation (MediationData):
\itemize{
\item \code{a}: Treatment -> Mediator (X -> M)
\item \code{b}: Mediator -> Outcome (M -> Y | X)
\item \code{c_prime}: Direct effect (X -> Y | M)
}

For serial mediation (SerialMediationData):
\itemize{
\item \code{a}: Treatment -> First mediator
\item \code{d21}, \code{d32}, ...: Mediator-to-mediator paths
\item \code{b}: Last mediator -> Outcome
\item \code{c_prime}: Direct effect
}
}
\examples{
# Generate example data
set.seed(123)
n <- 100
mydata <- data.frame(X = rnorm(n))
mydata$M <- 0.5 * mydata$X + rnorm(n)
mydata$Y <- 0.3 * mydata$X + 0.4 * mydata$M + rnorm(n)

med_data <- fit_mediation(
  formula_y = Y ~ X + M,
  formula_m = M ~ X,
  data = mydata,
  treatment = "X",
  mediator = "M"
)

paths(med_data)

}
\seealso{
\code{\link[=nie]{nie()}}, \code{\link[=nde]{nde()}}, \code{\link[=te]{te()}}, \code{\link[=pm]{pm()}}
}
