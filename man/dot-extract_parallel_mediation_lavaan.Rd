% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-lavaan.R
\name{.extract_parallel_mediation_lavaan}
\alias{.extract_parallel_mediation_lavaan}
\title{Extract Parallel Mediation Structure from a lavaan Model}
\usage{
.extract_parallel_mediation_lavaan(
  object,
  treatment,
  mediators,
  outcome = NULL,
  standardized = FALSE,
  ...
)
}
\arguments{
\item{object}{Fitted lavaan model.}

\item{treatment}{Character scalar: treatment variable name.}

\item{mediators}{Character vector (length >= 2): mediator names (any order;
the \code{a_j}/\code{b_j} indices follow this vector).}

\item{outcome}{Character scalar, or \code{NULL} to auto-detect (the common
non-mediator variable predicted by the mediators).}

\item{standardized}{Logical: extract standardized coefficients?}

\item{...}{Additional arguments (ignored).}
}
\value{
A \code{ParallelMediationData} object.
}
\description{
Internal worker for the parallel branch of \code{\link[=extract_mediation]{extract_mediation()}} on lavaan
objects (\code{X -> M_j -> Y} for k independent mediators). It is the SEM analogue
of \code{\link[=.extract_parallel_mediation_lm]{.extract_parallel_mediation_lm()}} and returns a \code{ParallelMediationData}
object. Total indirect effect = \verb{sum_j a_j * b_j}.
}
\details{
Paths are located in the lavaan parameter table by variable name:
\itemize{
\item \code{a_j}: \code{M_j ~ X}
\item \code{b_j}: \code{Y ~ M_j}
\item \verb{c'} : \code{Y ~ X} (defaults to 0 with a warning if absent -- full mediation)
}

Unlike the lm/glm engine -- where the \code{M_j} come from separate regressions so
\code{cov(a_j, b_j) = 0} -- lavaan estimates the whole system jointly, so the
expanded \code{vcov} preserves the FULL off-diagonal structure (including
\code{cov(a_j, b_j)} and \verb{cov(a_j, a_j')}). Downstream SEs therefore reflect the
true joint covariance; tests must not hardcode any of these to zero.
}
\keyword{internal}
