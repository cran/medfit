% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-lavaan.R
\name{.classify_multimediator_structure_lavaan}
\alias{.classify_multimediator_structure_lavaan}
\title{Classify a multi-mediator lavaan structure as serial or parallel}
\usage{
.classify_multimediator_structure_lavaan(
  object,
  mediators,
  standardized = FALSE
)
}
\arguments{
\item{object}{Fitted lavaan model.}

\item{mediators}{Character vector of mediator names (length >= 2).}

\item{standardized}{Logical: passed through for table selection (the rows
used for detection are identical, but keeping it consistent avoids a second
solver call surprising the caller).}
}
\value{
\code{"serial"} or \code{"parallel"}.
}
\description{
Conservative, backward-compatible inference for \code{structure = "auto"} on
lavaan objects -- the SEM analogue of \code{\link[=.classify_multimediator_structure]{.classify_multimediator_structure()}}
for lm/glm. Returns \code{"parallel"} only on POSITIVE evidence (no mediator is
regressed on another); otherwise defaults to \code{"serial"} (the historical
default for vector \code{mediator}). It never errors -- malformed inputs fall
through to the chosen worker's own directed validation. Users can always set
\code{structure} explicitly to override.
}
\details{
Detection reads only \code{op == "~"} (regression) rows, so residual covariances
(\verb{~~}) among mediators cannot masquerade as serial chain edges. Like the
lm/glm classifier, it returns \code{"parallel"} only on POSITIVE evidence: no
mediator-on-mediator edge AND every mediator enters a single common outcome
equation. Anything else (e.g. one mediator missing from the outcome model)
falls back to \code{"serial"}, preserving the historical vector-mediator behavior.
}
\keyword{internal}
