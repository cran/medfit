% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/med.R
\name{quick}
\alias{quick}
\title{Quick Summary of Mediation Results}
\usage{
quick(x, digits = 3, ...)
}
\arguments{
\item{x}{A MediationData object (or result from \code{\link[=med]{med()}})}

\item{digits}{Integer: number of significant digits (default: 3)}

\item{...}{Additional arguments (ignored)}
}
\value{
Invisibly returns x
}
\description{
Print a one-line summary of mediation results, perfect for quick checks
or ADHD-friendly workflows.
}
\details{
Prints a compact one-line summary showing:
\itemize{
\item NIE (Natural Indirect Effect) with CI if available
\item NDE (Natural Direct Effect)
\item Proportion Mediated (PM)
}

If bootstrap results are available (from \code{med(..., boot = TRUE)}),
confidence intervals are shown for NIE.
}
\examples{
# Generate example data
set.seed(123)
n <- 100
mydata <- data.frame(X = rnorm(n))
mydata$M <- 0.5 * mydata$X + rnorm(n)
mydata$Y <- 0.3 * mydata$X + 0.4 * mydata$M + rnorm(n)

result <- med(
  data = mydata,
  treatment = "X",
  mediator = "M",
  outcome = "Y"
)

# One-line summary
quick(result)

}
\seealso{
\code{\link[=med]{med()}}, \code{\link[=nie]{nie()}}, \code{\link[=nde]{nde()}}, \code{\link[=pm]{pm()}}
}
