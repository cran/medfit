% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-lavaan.R
\name{.extract_serial_mediation_lavaan}
\alias{.extract_serial_mediation_lavaan}
\title{Extract Serial Mediation Structure from a lavaan Model}
\usage{
.extract_serial_mediation_lavaan(
  object,
  treatment,
  mediators,
  outcome = NULL,
  standardized = FALSE,
  ...
)
}
\arguments{
\item{object}{Fitted lavaan model object.}

\item{treatment}{Character scalar: treatment variable name.}

\item{mediators}{Character vector (length >= 2): mediator names in causal
order (\code{M1 -> M2 -> ... -> Mk}).}

\item{outcome}{Character scalar, or \code{NULL} to auto-detect from the variable
predicted by the last mediator.}

\item{standardized}{Logical: extract standardized coefficients?}

\item{...}{Additional arguments (ignored).}
}
\value{
A \link{SerialMediationData} object.
}
\description{
Internal worker for the serial branch of \code{\link[=extract_mediation]{extract_mediation()}} on lavaan
objects. It is invoked by \code{\link[=extract_mediation_lavaan]{extract_mediation_lavaan()}} when \code{mediator} is a
character vector of length >= 2, and returns a \link{SerialMediationData} object
describing the chain X -> M1 -> M2 -> ... -> Mk -> Y.
}
\details{
Paths are located in the lavaan parameter table by variable name:
\itemize{
\item \code{a}  : \code{M1 ~ X}
\item \code{d_i}: \verb{M_\{i+1\} ~ M_i} for \verb{i = 1 .. k-1} (the \code{k - 1} inter-mediator paths)
\item \code{b}  : \code{Y ~ Mk}
\item \verb{c'} : \code{Y ~ X} (defaults to 0 with a warning if absent -- full mediation)
}

As in the simple-mediation extractor, named structural aliases
(\code{a}, \code{d1}, ..., \verb{d\{k-1\}}, \code{b}, \code{c_prime}) are appended to \code{estimates} and
the variance-covariance matrix is expanded so that the FULL covariance
row/column of each source parameter is preserved. This lets downstream code
recover the true joint covariance of the chain (including off-diagonals)
via, for example, \code{vcov[c("a", "d1", "b"), c("a", "d1", "b")]} -- which is
required for serial indirect-effect standard errors.
}
\keyword{internal}
