% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-lm.R
\name{.extract_interaction_mediation_lm}
\alias{.extract_interaction_mediation_lm}
\title{Extract Treatment-Mediator Interaction Structure from lm/glm Models}
\usage{
.extract_interaction_mediation_lm(
  model_m,
  model_y,
  treatment,
  mediator,
  int_term,
  outcome = NULL,
  data = NULL,
  m_star = 0
)
}
\arguments{
\item{model_y}{Fitted lm/glm for the outcome (\code{Y ~ Mk + X + ...}).}

\item{treatment}{Character scalar: treatment variable name.}

\item{int_term}{Character: the interaction coefficient name in \code{model_y}
(from \code{\link[=.find_interaction_term]{.find_interaction_term()}}).}

\item{outcome}{Character scalar, or \code{NULL} to auto-detect from \code{model_y}.}

\item{data}{Data frame, or \code{NULL} to take the \code{object} model frame.}

\item{m_star}{Numeric scalar reference mediator level.}
}
\value{
An \code{InteractionMediationData} object.
}
\description{
Internal worker for the four-way (VanderWeele 2014) branch of the lm/glm
\code{\link[=extract_mediation]{extract_mediation()}} method. Invoked by \code{\link[=.extract_mediation_lm_impl]{.extract_mediation_lm_impl()}} when a
single mediator's outcome model carries an \code{X:M} term. Builds an
\code{InteractionMediationData} object for continuous \code{Y} and \code{M} with binary
treatment (0 -> 1) and reference mediator level \code{m_star}.
}
\details{
With mediator model \eqn{M = \beta_0 + \beta_1 X + \beta_2^\top C}{M = b0 + b1*X + b2'C}
and outcome model
\eqn{Y = \theta_0 + \theta_1 X + \theta_2 M + \theta_3 XM + \dots}{Y = t0 + t1*X + t2*M + t3*XM + ...}
the components are CDE = \eqn{\theta_1 + \theta_3 m^*}{t1 + t3*m*},
INTref = \eqn{\theta_3 (E[M\mid X=0] - m^*)}{t3*(E[M|X=0] - m*)},
INTmed = \eqn{\theta_3 \beta_1}{t3*b1}, PIE = \eqn{\theta_2 \beta_1}{t2*b1},
where \eqn{E[M\mid X=0]}{E[M|X=0]} evaluates covariates at their sample means.
The combined \code{vcov} is block-diagonal across the two separately-fitted
equations and named with the aliases \code{a}, \code{b}, \code{c_prime}, \code{theta3}, \code{b0}.
}
\keyword{internal}
