% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/med.R
\name{med}
\alias{med}
\title{Simple Mediation Analysis}
\usage{
med(
  data,
  treatment,
  mediator,
  outcome,
  covariates = NULL,
  boot = FALSE,
  n_boot = 1000L,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame containing all variables}

\item{treatment}{Character: name of treatment (exposure) variable}

\item{mediator}{Character: name of mediator variable}

\item{outcome}{Character: name of outcome variable}

\item{covariates}{Character vector: names of covariates to include
(optional, default: none)}

\item{boot}{Logical: compute bootstrap confidence intervals?
(default: FALSE for speed)}

\item{n_boot}{Integer: number of bootstrap samples (default: 1000)}

\item{seed}{Integer: random seed for reproducibility (optional)}

\item{...}{Additional arguments passed to \code{\link[=fit_mediation]{fit_mediation()}}}
}
\value{
A MediationData object with mediation results
}
\description{
A simplified entry point for mediation analysis. Specify the data and
variable names, and get results with minimal configuration.

This is the recommended starting point for most mediation analyses.
For more control over model specifications, use \code{\link[=fit_mediation]{fit_mediation()}} directly.
}
\details{
\code{med()} is designed to be the simplest way to run a mediation analysis.
It constructs the model formulas automatically from variable names.
\subsection{Default Behavior}{
\itemize{
\item Fits Gaussian (continuous) mediator and outcome models
\item No covariates unless specified
\item No bootstrap unless requested (use \code{boot = TRUE})
}
}

\subsection{Accessing Results}{

After running \code{med()}, use:
\itemize{
\item \code{nie(result)}: Natural indirect effect
\item \code{nde(result)}: Natural direct effect
\item \code{te(result)}: Total effect
\item \code{pm(result)}: Proportion mediated
\item \code{quick(result)}: One-line summary
\item \code{summary(result)}: Detailed summary
}
}
}
\examples{
# Generate example data
set.seed(123)
n <- 200
mydata <- data.frame(
  treatment = rnorm(n),
  covariate = rnorm(n)
)
mydata$mediator <- 0.5 * mydata$treatment + 0.2 * mydata$covariate + rnorm(n)
mydata$outcome <- 0.3 * mydata$treatment + 0.4 * mydata$mediator +
                  0.1 * mydata$covariate + rnorm(n)

# Simple mediation (no covariates)
result <- med(
  data = mydata,
  treatment = "treatment",
  mediator = "mediator",
  outcome = "outcome"
)
print(result)

# With covariates
result_cov <- med(
  data = mydata,
  treatment = "treatment",
  mediator = "mediator",
  outcome = "outcome",
  covariates = "covariate"
)

# Quick summary
quick(result)

\donttest{
# With bootstrap CI (slower)
result_boot <- med(
  data = mydata,
  treatment = "treatment",
  mediator = "mediator",
  outcome = "outcome",
  boot = TRUE,
  n_boot = 1000,
  seed = 42
)
}

}
\seealso{
\code{\link[=fit_mediation]{fit_mediation()}} for full control, \code{\link[=quick]{quick()}} for instant summary,
\code{\link[=nie]{nie()}}, \code{\link[=nde]{nde()}}, \code{\link[=te]{te()}}, \code{\link[=pm]{pm()}} for extracting effects
}
