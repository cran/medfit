% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-lm.R
\name{.extract_serial_mediation_lm}
\alias{.extract_serial_mediation_lm}
\title{Extract Serial Mediation Structure from lm/glm Models}
\usage{
.extract_serial_mediation_lm(
  object,
  mediator_models,
  model_y,
  treatment,
  mediators,
  outcome = NULL,
  data = NULL
)
}
\arguments{
\item{object}{Fitted lm/glm for the first mediator (\code{M1 ~ X + ...}).}

\item{mediator_models}{List (length \code{k - 1}) of fitted lm/glm models for
mediators 2..k (\code{M2 ~ M1 + ...}, ..., \code{Mk ~ M(k-1) + ...}), in chain order.}

\item{model_y}{Fitted lm/glm for the outcome (\code{Y ~ Mk + X + ...}).}

\item{treatment}{Character scalar: treatment variable name.}

\item{mediators}{Character vector (length >= 2): mediator names in causal
order (\code{M1 -> M2 -> ... -> Mk}).}

\item{outcome}{Character scalar, or \code{NULL} to auto-detect from \code{model_y}.}

\item{data}{Data frame, or \code{NULL} to take the \code{object} model frame.}
}
\value{
A \link{SerialMediationData} object.
}
\description{
Internal worker for the serial branch of the lm/glm \code{\link[=extract_mediation]{extract_mediation()}}
method. Invoked by \code{\link[=.extract_mediation_lm_impl]{.extract_mediation_lm_impl()}} when \code{mediator} is a
character vector of length >= 2. It assembles a \link{SerialMediationData} object
for the chain \code{X -> M1 -> M2 -> ... -> Mk -> Y} from \code{k + 1} separately
fitted regressions.
}
\details{
Path resolution: \code{a} = coefficient of \code{treatment} in \code{object}; \code{d_i} =
coefficient of \code{mediators[i]} in \code{mediator_models[[i]]} (the predecessor
mediator, read regardless of any additional covariates in that equation);
\code{b} = coefficient of \code{mediators[k]} in \code{model_y}; \verb{c'} = coefficient of
\code{treatment} in \code{model_y} (0 with a warning if absent).

The combined \code{vcov} is block-diagonal across the separately-fitted equations
(so \code{cov(a, d_i) = cov(d_i, b) = 0}) but preserves the within-\code{model_y}
covariance, so \verb{cov(b, c')} is non-zero. See the \code{extract_mediation} lm
method docs for the lm-vs-lavaan covariance divergence this implies.
}
\keyword{internal}
