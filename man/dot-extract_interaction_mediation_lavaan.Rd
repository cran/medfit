% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-lavaan.R
\name{.extract_interaction_mediation_lavaan}
\alias{.extract_interaction_mediation_lavaan}
\title{Extract Treatment-Mediator Interaction Structure from a lavaan Model}
\usage{
.extract_interaction_mediation_lavaan(
  object,
  treatment,
  mediator,
  int_term,
  outcome = NULL,
  m_star = 0,
  standardized = FALSE
)
}
\arguments{
\item{object}{Fitted lavaan model object.}

\item{treatment}{Character scalar: treatment variable name.}

\item{int_term}{Character: the interaction (product) coefficient name in the
outcome equation.}

\item{outcome}{Character scalar, or \code{NULL} to auto-detect from the variable
predicted by the last mediator.}

\item{m_star}{Numeric scalar reference mediator level.}

\item{standardized}{Logical: extract standardized coefficients?}
}
\value{
An \code{InteractionMediationData} object.
}
\description{
Internal worker for the four-way (VanderWeele 2014) branch of
\code{\link[=extract_mediation]{extract_mediation()}} on lavaan objects. The SEM analogue of
\code{\link[=.extract_interaction_mediation_lm]{.extract_interaction_mediation_lm()}}: it returns an \code{InteractionMediationData}
object for continuous \code{Y} and \code{M} with binary treatment and reference level
\code{m_star}.
}
\details{
Because lavaan fits one joint system, the expanded \code{vcov} preserves the FULL
covariance among the paths -- including \code{cov(beta1, theta3)} and
\code{cov(beta0, theta3)} -- unlike the block-diagonal lm/glm engine, so the
delta-method standard errors reflect the joint estimation. The mediator
intercept \code{beta0} (needed for INTref) is read from the \code{~1} row, so the model
must be fit with \code{meanstructure = TRUE}.
}
\keyword{internal}
