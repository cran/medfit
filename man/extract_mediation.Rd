% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aab-generics.R
\name{extract_mediation}
\alias{extract_mediation}
\title{Extract Mediation Structure from Fitted Models}
\usage{
extract_mediation(object, ...)
}
\arguments{
\item{object}{Fitted model object (lm, glm, lavaan, etc.)}

\item{...}{Additional arguments passed to methods. Common arguments include:
\itemize{
\item \code{treatment}: Character string specifying treatment variable name
\item \code{mediator}: Character string specifying mediator variable name
\item Method-specific arguments (see individual method documentation)
}}
}
\value{
A \link{MediationData} object containing:
\itemize{
\item Path coefficients (a, b, c')
\item Full parameter vector and variance-covariance matrix
\item Residual variances (for Gaussian models)
\item Variable names and metadata
\item Original data (if available)
}
}
\description{
Generic function to extract mediation structure (a, b, c' paths and
variance-covariance matrices) from fitted models. This function provides
a unified interface for extracting mediation information from various
model types (lm, glm, lavaan, lmer, brms, etc.).
}
\details{
The \code{extract_mediation()} generic provides methods for different model types:
\itemize{
\item \strong{lm/glm}: Extract from linear and generalized linear models
\item \strong{lavaan}: Extract from structural equation models
\item \strong{lmerMod}: Extract from mixed-effects models (future)
\item \strong{brmsfit}: Extract from Bayesian models (future)
}

Note: OpenMx extraction is planned for a future release.

All methods return a standardized \link{MediationData} object that can be used
with other medfit functions and dependent packages (probmed, RMediation,
medrobust).
}
\examples{
\donttest{
# Simulate data with a single mediator (X -> M -> Y)
set.seed(123)
n <- 200
X <- rnorm(n)
M <- 0.5 * X + rnorm(n)
Y <- 0.3 * M + 0.2 * X + rnorm(n)
dat <- data.frame(X = X, M = M, Y = Y)

# Extract the mediation structure from fitted lm models
fit_m <- lm(M ~ X, data = dat)
fit_y <- lm(Y ~ X + M, data = dat)
med_data <- extract_mediation(fit_m, model_y = fit_y,
                              treatment = "X", mediator = "M")
}

}
\seealso{
\link{MediationData}, \code{\link[=fit_mediation]{fit_mediation()}}, \code{\link[=bootstrap_mediation]{bootstrap_mediation()}}
}
