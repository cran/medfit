% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{ParallelMediationData}
\alias{ParallelMediationData}
\title{ParallelMediationData: Parallel (Multiple-Mediator) Mediation Structure}
\usage{
ParallelMediationData(a_paths, b_paths, c_prime, estimates, vcov,
  sigma_mediators, sigma_y, treatment, mediators, outcome,
  mediator_predictors, outcome_predictors, data, n_obs, converged,
  source_package)
}
\arguments{
\item{a_paths}{Numeric vector: treatment -> mediator effects
\eqn{(a_1, \dots, a_k)}{(a_1, ..., a_k)}.}

\item{b_paths}{Numeric vector: mediator -> outcome effects
\eqn{(b_1, \dots, b_k)}{(b_1, ..., b_k)}; must be the same length as \code{a_paths}.}

\item{c_prime}{Numeric scalar: direct effect \eqn{X \rightarrow Y}{X -> Y}.}

\item{estimates}{Numeric vector of all parameter estimates.}

\item{vcov}{Square variance-covariance matrix of \code{estimates}.}

\item{sigma_mediators}{Optional numeric vector of mediator residual SDs (length k), or NULL.}

\item{sigma_y}{Optional numeric scalar outcome residual SD, or NULL.}

\item{treatment, outcome}{Single character strings naming the treatment / outcome.}

\item{mediators}{Character vector of mediator names (length k, unique).}

\item{mediator_predictors}{List of predictor-name vectors, one per mediator.}

\item{outcome_predictors}{Character vector of outcome-model predictor names.}

\item{data}{Optional data frame, or NULL.}

\item{n_obs}{Integer number of observations.}

\item{converged}{Logical convergence flag.}

\item{source_package}{Character name of the originating package.}
}
\value{
A \code{ParallelMediationData} S7 object.
}
\description{
S7 class for \strong{parallel} mediation, where a treatment affects an outcome
through two or more \emph{independent} mediators operating in parallel
(\eqn{X \rightarrow M_j \rightarrow Y}{X -> M_j -> Y} for
\eqn{j = 1, \dots, k}{j = 1, ..., k}). The total indirect effect is the sum
of the per-mediator products, \eqn{\sum_{j=1}^{k} a_j b_j}{sum(a_j * b_j)}.
This complements \link{MediationData} (simple) and \link{SerialMediationData}
(serial chains).
}
\examples{
pmd <- ParallelMediationData(
  a_paths = c(0.5, 0.4),
  b_paths = c(0.6, 0.3),
  c_prime = 0.2,
  estimates = c(0.5, 0.4, 0.6, 0.3, 0.2),
  vcov = diag(0.01, 5),
  treatment = "X",
  mediators = c("M1", "M2"),
  outcome = "Y",
  mediator_predictors = list("X", "X"),
  outcome_predictors = c("X", "M1", "M2"),
  n_obs = 200L,
  converged = TRUE,
  source_package = "medfit"
)

nie(pmd)   # total indirect effect: sum(a_j * b_j) = 0.42
paths(pmd) # a1, b1, a2, b2, c_prime

}
